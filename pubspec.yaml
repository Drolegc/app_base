name: only_sugar_admin
description: Administrator panel for Only Sugar

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  get: ^4.5.1
  cupertino_icons: ^1.0.2
  shared_preferences: ^2.0.5
  fluttertoast: ^8.0.3
  flutter_easyloading: ^3.0.0
  flutter_bootstrap: ^2.0.0
  either_dart: ^0.1.4

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/img/

